import { InventoryItemDTO } from './inventory-item-dto.model';

describe('InventoryItemDTO', () => {
  it('should create an instance', () => {
    expect(new InventoryItemDTO()).toBeTruthy();
  });
});
